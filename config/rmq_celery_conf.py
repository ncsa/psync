# This file serves two purposes:
# First, it contains the rmq url when the rmq server is running.
# Second, it contains additional rmq-related celery settings that will get
# used by celery IF the celery broker is set to rmq.

###
# RMQ specific settings
###

# RMQ URL information will be filled in below when the RMQ server starts
